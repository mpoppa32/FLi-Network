---
description: Run the session post-mortem ritual — log failures, propose rules, update context
---

Run the post-mortem ritual for this session:
1. Summarize: what was attempted, what shipped, what failed or went sideways.
2. For each failure: root cause in one sentence; write it to corsair-ops-log-v1.md with
   a one-line DO NOT REPEAT.
3. Propose at most 1–2 candidate RULES for CLAUDE.md — only if something actually broke
   that a rule would have prevented. Show me before adding (slow growth, Rule 10).
4. Update corsair-ops-context-v1.md with current state + the single next move.
